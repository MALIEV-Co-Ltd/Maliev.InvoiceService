apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    run: maliev-invoiceservice-api
  name: maliev-invoiceservice-api
  namespace: maliev
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    name: http
  selector:
    run: maliev-invoiceservice-api
  sessionAffinity: None
  type: NodePort